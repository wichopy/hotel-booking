export class Guest {
  constructor (guest) {
    this.id = guest.id
    this.name = guest.name
  }
}

export class Room {
  constructor (room) {
    this.id = room.id
    this.number = room.number
  }
}
